import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    linearGradient: {
        borderRadius: 8,
        padding: 10,
      },
    textButton:{
      color: 'white',
       textAlign: 'center',
        fontSize: 20,
    },
    Button:{
    marginBottom:100,
    width: 360,
    margin: 10,
  },
  });

export default styles;